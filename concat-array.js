const salaPython = ['Melissa', 'Otis', 'Hannah']

const salaJavaScript = ['Fernanda', 'Mirela', 'Mandy']

const salasUnificadas = salaPython.concat(salaJavaScript)

console.log(salasUnificadas)

//<concat> não mexe na array original.